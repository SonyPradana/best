<?php

should('return true (pre-memory)', function () {
    expect(true)->toBeTrue();
});

verify('order status (pre-memory)', function () {
    expect(true)->toBeTrue();
});

check('database connection (pre-memory)', function () {
    expect(true)->toBeTrue();
});
